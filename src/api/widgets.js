// const Widgets = new Mongo.Collection('widgets');

// Meteor.methods({
//   'widgets.fetch'() { return Widgets.find().fetch(); },
//   'widgets.insert'(data) {  Widgets.update({_id:data._id},data,{upsert:true}); return Widgets.find().fetch(); },  
//   'widgets.delete'(id) { Widgets.remove({_id:id}); return Widgets.find().fetch(); },
// })

// export default Widgets;
